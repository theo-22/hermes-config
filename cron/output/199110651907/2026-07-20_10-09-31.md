# Cron Job: browser-extension-update-check

**Job ID:** 199110651907
**Run Time:** 2026-07-20 10:09:31
**Schedule:** 0 10 * * 1

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Check for updates to the Hermes Browser Extension at /Users/ted/Hermes/hermes-browser-extension/.

1. Run: cd /Users/ted/Hermes/hermes-browser-extension && git fetch origin main
2. Compare local HEAD with origin/main
3. If there are new commits, report:
   - Number of new commits
   - Commit messages (summary)
   - Whether a rebuild is needed
4. If no updates, report "No updates available"
5. Do NOT auto-update or rebuild — just report what's available

## Response

## Hermes Browser Extension — Update Report

**Date:** 2026-07-20
**Status:** Updates available

### Summary

**71 new commits** are available on `origin/main`. The remote branch has been force-pushed (history rewritten) — local HEAD (`673805b`) is not an ancestor of `origin/main` (`f196da1`).

| Metric | Value |
|--------|-------|
| Local HEAD | `673805b` — "Update Hermes browser extension sidepanel" |
| Origin main | `f196da1` — "fix: restore Hermes Web model discovery fallbacks" |
| Commits behind | **71** |
| Local version | v0.1.6 |
| Upstream version | v0.1.6 + 83 commits |
| Force push | Yes (history was rewritten) |

### Commit Highlights (recent → older)

**Latest batch (top 15, most recent first):**
1. `f196da1` — fix: restore Hermes Web model discovery fallbacks
2. `b6125eb` — fix: harden full-tab runtime contracts
3. `866f1a3` — fix: hash build sources through Git filters
4. `5a54557` — fix: make Browser update status exact
5. `bb38cac` — feat: polish Browser startup and runtime controls
6. `fd28705` — fix: confirm sends to shared sessions
7. `11d4dd4` — fix: preserve ordered list numbering
8. `93af516` — fix: hide internal context wrappers in transcripts
9. `80d89e4` — fix: bound only the local transcript cache
10. `b4c6faf` — fix: preserve restored session history
11. `dd00816` — fix: improve Cyberpunk globe and refresh visual tour
12. `71dcf68` — feat: polish Hermes Browser loading experience
13. `0b577e5` — fix: confirm visible side-panel opens
14. `653ad3e` — revert: avoid consuming side-panel user gesture
15. `ba8372a` — fix: bypass hidden Arc side-panel contexts

**Notable earlier additions:**
- **Firefox preview build support** (`8917f20`)
- **Opera sidebar support + scoped element picker** (`c64b6cc`)
- **Backend-acknowledged session model lock (Model Scope Lock v2)** (`958beb2`)
- **Three-mode gateway connections** (`f729cf6`)
- **Remote dashboard session persistence across reconnects** (`2e8749c`)
- **Security hardening:** URL privacy contributions (`8ed7735`), session menu labels (`50c8f76`), browser security helpers (`bc58dde`), scrollbar/font settings fixes
- **Contributor credits ledger** added (`0e798d3`)

### Rebuild Assessment

**Rebuild is likely needed.** The change log includes:
- Build system change: `fix: hash build sources through Git filters` (affects build output integrity)
- New platform targets: Firefox build support, Opera sidebar
- Gateway architecture changes (three-mode connections, dashboard bridge)
- Build/package scripts exist: `npm run build` and `npm run package`

### Action Required

This is NOT an auto-update. To update and rebuild:
```bash
cd /Users/ted/Hermes/hermes-browser-extension
git reset --hard origin/main    # required due to forced push / rewritten history
npm install
npm run build
npm run verify
```

**Note:** Because the remote history was force-pushed, a simple `git pull` will fail. A `git reset --hard origin/main` (or `git rebase`) is required. The old local commits (`673805b` and its ancestors) are no longer part of upstream history and will be discarded.
