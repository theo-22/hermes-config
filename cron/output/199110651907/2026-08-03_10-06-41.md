# Cron Job: browser-extension-update-check

**Job ID:** 199110651907
**Run Time:** 2026-08-03 10:06:41
**Schedule:** 0 10 * * 1

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Check for updates to the Hermes Browser Extension at /Users/ted/Hermes/hermes-browser-extension/.

1. Run: cd /Users/ted/Hermes/hermes-browser-extension && git fetch origin main
2. Compare local HEAD with origin/main
3. If there are new commits, report:
   - Number of new commits
   - Commit messages (summary)
   - Whether a rebuild is needed
4. If no updates, report "No updates available"
5. Do NOT auto-update or rebuild — just report what's available

## Response

## Hermes Browser Extension — Updates Available

**Status: 85 new commits on `origin/main` behind local (local is also 1 commit ahead).**

### What changed
- **Local HEAD:** `673805b`
- **Remote HEAD:** `a22ed78` (fetched just now)
- **New commits:** 85
- **Diff scale:** 188 files changed, +41,572 / −1,677 lines

### Key commits (highlights)
| Commit | Date | Summary |
|---|---|---|
| `a22ed78` | 08-01 | fix: restore complete cloud session history |
| `422aabd` | 08-01 | fix: reuse existing surface for wake turns |
| `5f13ba2` | 07-31 | feat: persist cloud sessions and add wake word |
| `57510e5` | 07-30 | fix: load cloud preview model providers |
| `cb7a10f` | 07-30 | fix: harden model selection and voice dictation |
| `51d704b` | 07-29 | fix: authorize companion tools from shared hook ids |
| `e6298e3` | 07-21 | **feat: release Hermes Browser Extension v0.2.0** |
| `3786110` | — | feat: ship Hermes Web Alpha v0.1.11 |
| `312e85e` | — | chore: keep release-only artifacts out of source |

Full range spans v0.1.6 → v0.2.0, including multiple release markers (v0.1.7–v0.2.0), build-system changes (`scripts/build.mjs`, `build-firefox.mjs`, `build-content-runtime.mjs`), manifest updates, gateway/cloud session features, and 40+ bug fixes.

### Rebuild needed: **Yes**
- **Manifest version:** local `0.1.6` → remote `0.2.0`
- **Minimum Chrome version:** bumped 114 → 116
- Build scripts and `manifest.json` were modified; the local `dist/` was built from 0.1.6 and is stale.
- To update: pull (`git pull origin main` — note you're 1 commit ahead locally, so reconcile that first), then run `npm run build` / the applicable build script and reload the extension.

### Action taken
None — per instructions, no auto-update or rebuild was performed. This is report-only.

**Recommendation:** Pull and rebuild when convenient; the v0.2.0 release includes wake-word support and cloud session persistence, and the current installed build (0.1.6) is two minor versions behind.
