# Cron Job: browser-extension-update-check

**Job ID:** 199110651907
**Run Time:** 2026-08-10 10:08:10
**Schedule:** 0 10 * * 1

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Check for updates to the Hermes Browser Extension at /Users/ted/Hermes/hermes-browser-extension/.

1. Run: cd /Users/ted/Hermes/hermes-browser-extension && git fetch origin main
2. Compare local HEAD with origin/main
3. If there are new commits, report:
   - Number of new commits
   - Commit messages (summary)
   - Whether a rebuild is needed
4. If no updates, report "No updates available"
5. Do NOT auto-update or rebuild — just report what's available

## Response

**Update available** — 120 new commits on `origin/main` for the Hermes Browser Extension.

## Summary

| Item | Value |
|---|---|
| Local HEAD | `673805b` ("Update Hermes browser extension sidepanel") |
| origin/main | `64df7b1` (tagged **v0.2.0**) |
| New commits behind | **120** |
| Local-only commits | **1** (`673805b` — not on origin; merge will need care) |
| Files changed | 302 files, +284,303 / −2,033 |
| Manifest version | 0.1.6 → **0.2.0** |

## What's in the update (commit theme summary)

- **v0.2.0 release line** — VS Code theme marketplace with Agent Theme Studio, validated custom theme imports, cross-surface readability controls, settings polish
- **New platform support** — Firefox preview build support, Opera sidebar + scoped element picker, generated image diffusion reveal
- **Session/model improvements** — backend-acknowledged session model lock (Model Scope Lock v2), preserved canonical model catalog across gateway updates, bind runtime options to browser sessions, prevent duplicate turn retries
- **Connection/auth hardening** — first-party dashboard profile discovery, remote session auth diagnostics, fail-closed roster, suppress adjacent-port agent scan in remote-dashboard mode
- **UI fixes** — model picker above dock, Hermes scrollbar on bottom dock, text size readability, command hover detail cards
- **Security/cleanup** — hardened session menu labels, security helper hardening, removed failing GitHub Actions workflow, gitignore bytecode
- Also includes prior release lines v0.1.7 → v0.1.10 (browser context protocol extraction, runtime controls, supportability layer)

## Rebuild needed: **YES**

This is a significant upgrade, not a patch:
1. **Manifest version bump** 0.1.6 → 0.2.0, and manifest restructured to use i18n `__MSG_*__` placeholders (`default_locale: en` added)
2. **New required permissions**: `contextMenus`, `downloads`, `offscreen` added; `audioCapture` moved out of optional_permissions
3. **Minimum Chrome version raised** 114 → 116
4. Local `dist/` is a packaged build — with 302 files changed and 284K insertions, the current build artifact is stale

## Not done (per instructions)
- No auto-update, no rebuild, no merge. Local is 1 commit ahead of origin, so a `git pull` will need a merge/rebase decision for `673805b` before rebuilding.

**Next step for Ted:** `git pull origin main` (resolve the 1 local commit), then rebuild `dist/` and reload the extension in the browser — Chrome minimum is now 116.
