# Cron Job: browser-extension-update-check

**Job ID:** 199110651907
**Run Time:** 2026-08-31 10:01:35
**Schedule:** 0 10 * * 1

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Check for updates to the Hermes Browser Extension at /Volumes/Extra/Substrate/Hermes/hermes-browser-extension/.

1. Check the latest published version of the extension (store listing or GitHub releases if configured).
2. Read the local manifest at /Volumes/Extra/Substrate/Hermes/hermes-browser-extension/manifest.json (or package.json) for the current local version.
3. If a newer version exists, report the version delta and the upgrade path. If current, say so in one line.
4. Do not modify any files — report only.

## Response

Update check complete — verified against local files and the live GitHub repo. Report only, no files touched.

## Hermes Browser Extension — Update Check (2026-08-31)

**Verdict: OUTDATED — update available.**

| Surface | Version |
|---|---|
| Local manifest.json / package.json / dist/manifest.json (installed build) | **0.1.6** (built 2026-06-30 from commit `b6500d7`) |
| Latest GitHub release (abundantbeing/hermes-browser-extension) | **v0.3.1** (published 2026-08-27) |

**Version delta: 0.1.6 → 0.3.1** — local repo is **behind origin/main by 129 commits**, spanning three releases:
- **v0.2.0** (Jul 21): "Hermes Assist" site-aware drafting surface
- **v0.3.0** (Aug 21): opt-in permissioned browser-tab control plane (68 commits — largest release)
- **v0.3.1** (Aug 27): stability — fixes content-script crash class + restores **automatic gateway pairing on auth-hardened gateways** (directly relevant: Ted's gateway runs with `API_SERVER_KEY` set, the exact failure mode this fixes)

**Local state to preserve:** repo is 1 commit ahead (`673805b`) containing Ted's sidepanel.css font-size mods (body 20px / message-content 18px / UI 15px) — will conflict on update.

**Upgrade path (not executed — report only):**
1. Save the CSS mods: `git stash` (or note the sidepanel.css diff from `673805b`)
2. `git pull --rebase origin main` → re-apply font-size customizations to `extension/sidepanel.css` on the new code
3. `npm install && npm run build`
4. Reload unpacked `dist/` in `chrome://extensions`
5. Verify extension ↔ gateway (port 8642) pairing still works — v0.3.x added a new control-plane surface, so confirm the local Hermes gateway accepts it before relying on it; roll back via git checkout if pairing breaks.

Recommend scheduling the upgrade as a bounded task rather than a cron auto-action (CSS conflict resolution + gateway pairing verification need a live check).
