# Cron Job: browser-extension-update-check

**Job ID:** 199110651907
**Run Time:** 2026-09-07 10:04:12
**Schedule:** 0 10 * * 1

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Check for updates to the Hermes Browser Extension at /Volumes/Extra/Substrate/Hermes/hermes-browser-extension/.

1. Check the latest published version of the extension (store listing or GitHub releases if configured).
2. Read the local manifest at /Volumes/Extra/Substrate/Hermes/hermes-browser-extension/manifest.json (or package.json) for the current local version.
3. If a newer version exists, report the version delta and the upgrade path. If current, say so in one line.
4. Do not modify any files — report only.

## Response

**Hermes Browser Extension is 9 releases behind — update available.**

## Version delta
| | Version | When |
|---|---|---|
| Local (manifest.json, package.json, build-info.json all agree) | **0.1.6** | built 2026-06-30, commit `b6500d7` |
| Latest published release (GitHub Releases — no store listing; primary install is unpacked load) | **v0.3.2** | published 2026-09-05 (2 days ago) |
| Remote `main` | `176fdec` | already ahead of v0.3.2 (one unreleased Bot-Mode discovery fix, #99) |

Releases in between: 0.1.7 → 0.1.8 → 0.1.9 → 0.1.10 → 0.1.11 → 0.2.0 → 0.3.0 → 0.3.1 → 0.3.2.

## What's in the gap (biggest items)
- **v0.3.0 (08-22)** — opt-in live browser control: authenticated MV3 controller, exact per-tab leases, approval gates, artifact upload/download with SHA-256 + TTL, Firefox signing support.
- **v0.3.1 (08-27)** — Vivaldi/Chromium `Identifier already declared` crash fix; pairing fixes for auth-hardened gateways; startup latency instrumentation.
- **v0.3.2 (09-05)** — Hermes Bot Mode (multi-agent roster, instant profile switching), live Group Chat & Threads, AI tab triage (`/sort-tabs`), fixes for the 40-second connection delay, paged session loading, and a truthful tab-context scope (`Page only` no longer leaks unselected tabs to the model).

## Upgrade path (README-documented, unpacked install)
1. `git pull` in `/Volumes/Extra/Substrate/Hermes/hermes-browser-extension/` (or download the `v0.3.2` chromium artifact from Releases — `SHA256SUMS-v0.3.2.txt` is provided for verification).
2. `npm install` then `npm run build`.
3. Reload the unpacked extension card in the browser's extensions page. If it still shows 0.1.6: **Remove**, then **Load unpacked** again pointing at the fresh `dist/` folder.

Notes: the local checkout's git HEAD is dated 2026-07-15 — newer source than the 0.1.6 build but never rebuilt, and its manifest still reads 0.1.6 (no version drift, just an unbumped checkout). The build-time `dirty: true` flag in build-info.json is stale; the working tree is currently clean. No store auto-update exists for this extension — unpacked loads only update via the manual build+reload step above, so it will keep drifting without a check.

No files were modified — report only.
