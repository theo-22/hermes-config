# Cron Job: browser-extension-update-check

**Job ID:** 199110651907
**Run Time:** 2026-09-14 10:01:06
**Schedule:** 0 10 * * 1

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Check for updates to the Hermes Browser Extension at /Volumes/Extra/Substrate/Hermes/hermes-browser-extension/.

1. Check the latest published version of the extension (store listing or GitHub releases if configured).
2. Read the local manifest at /Volumes/Extra/Substrate/Hermes/hermes-browser-extension/manifest.json (or package.json) for the current local version.
3. If a newer version exists, report the version delta and the upgrade path. If current, say so in one line.
4. Do not modify any files — report only.

## Response

## Hermes Browser Extension — Update Check

**Local:** `0.1.6` (manifest.json + package.json, commit `673805b` dated Jun 29)
**Latest published:** `v0.3.2` (GitHub, Sep 5 2026 — "last week")

**Delta:** 0.1.6 → 0.3.2 — nearly a full major revision behind. v0.3.2 adds: Hermes Bot Mode with multi-agent roster & instant profile switching, live Group Chat/Threads, AI Tab Triage, truthful tab context scoping, zero-config dynamic Desktop discovery, session paging fix, 1,385 tests passing.

**Upgrade path:**
```
cd /Volumes/Extra/Substrate/Hermes/hermes-browser-extension
git pull origin main
npm install
npm run build            # unpacked Chromium build in dist/
npm run package          # tar.gz artifact for Chrome/Firefox install
```

**Note:** local git log only goes back to Jun 29 (5 commits) while upstream has 14 releases — the local repo may be shallow or on a stale branch. Verify remote branches after `git fetch` before pulling.

No files modified.
