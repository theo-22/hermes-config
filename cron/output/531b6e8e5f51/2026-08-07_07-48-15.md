# Cron Job: substrate-morning-briefing

**Job ID:** 531b6e8e5f51
**Run Time:** 2026-08-07 07:48:15
**Schedule:** 45 7 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

You are Substrate Monitor, Ted's system health operator. Run the morning briefing consolidation.

Your job: read the overnight data outputs and the live system surfaces, synthesize them into one structured briefing for Ted, and write it to `/Users/ted/Operations/Substrate_Morning_Briefing.md`.

## What to read

1. `/Users/ted/Operations/Substrate_Morning_Consolidated.md` — raw findings from the no-agent overnight run (room drift, proof gaps, research returns, thread/room status). If this file does not exist or is empty, note "No overnight findings — system is clean."
2. `/Users/ted/Operations/reports/Substrate_Rollup_LATEST.md` — system-level state summary
3. Latest meta_agent report: `ls -t /Users/ted/Operations/reports/meta_agent_*.md | head -1` then read it
4. Session chain: `/Users/ted/Operations/session_chains/CONDUCTOR.md` and `/Users/ted/Operations/session_chains/00_START_HERE.md`
5. QuickSave receipts dir: `ls -t /Users/ted/Operations/QuickSave_Receipts/*.md 2>/dev/null | head -3`
6. Research Coordination queue: `/Users/ted/Projects/Research_Coordination/01_Research_Queue.md` — check for returned items
7. Cron health: read the output of `cronjob action='list'` — just check for erroring jobs (last_status != 'ok')
8. Contradiction check: `/Users/ted/Operations/reports/substrate_contradiction_check_*.md` — latest instance if it ran

## Output structure

Write the briefing to `/Users/ted/Operations/Substrate_Morning_Briefing.md` with this structure:

```markdown
# Substrate Morning Briefing — YYYY-MM-DD

**Status:** GREEN / YELLOW / RED

## 1. Overnight Summary
One-paragraph summary of what the overnight batch found. If nothing, say "Clean — no drift, no proof gaps, no stale threads."

## 2. Grouped Findings

| Priority | Finding | Source | Can auto-fix? |
|----------|---------|--------|---------------|
| (high/med/low) | Description | which surface | yes/no |

Group by: system health (crons), room drift (stale rooms), proof gaps, research returns, session chain staleness. Max 10 items — if nothing needs attention, say so.

## 3. Proposed Fixes
For each finding marked "yes" above, state the bounded auto-fix clearly:
- What file would change
- What the change would be
- Why it's safe (reversible, within domain)

## 4. Questions for Ted
Only items that genuinely need a decision. Max 3. If none, say "No decisions needed — all findings have clear auto-fix paths or are informational."

## 5. Approval Gate
```
**Gate:** No actions executed. If Ted approves, run: [concise next step]
```

## Rules
- Produce the briefing even if findings are empty — "Clean" is a useful report
- Keep it concise. Ted reads fast.
- Label inferences clearly ("appears to be", "likely caused by")
- Do NOT mutate any files beyond the briefing output
- `openrouter/free` is the model router
- If a source file is missing or unreadable, skip it gracefully — don't fail


## Response

Briefing is written and verified on disk. Here's the delivered report:

---

# Substrate Morning Briefing — 2026-08-07

**Status:** YELLOW

## 1. Overnight Summary
Overnight batch ran clean — all 5 owned crons `ok`, 0 errored jobs in 24h, watchdog silent, volume mounted. No proof gaps, no returned research pending absorption, no contradiction-check instance on file. The content is room drift: 18 rooms need honest state refresh, and one dashboard room record points at a directory that no longer exists. Two environment items need eyes: disk below its free-space floor (20.6 GiB vs 22 GiB), and a symlink guard alerting on the same 7 Claude hooks for 5 consecutive days. *(Skipped source: `Operations/QuickSave_Receipts/` doesn't exist — no receipts surfaced.)*

## 2. Grouped Findings
| Priority | Finding | Can auto-fix? |
|----------|---------|---------------|
| high | `System13_Plugin_Candidate_Trial` — dashboard DB record references room, directory missing everywhere (verified). Appears to be stale record from a deleted/moved room. | no (DB write needs sign-off) |
| med | 18 Project Rooms flagged: 14 need honest state refresh, 4 active rooms stale 18–24d, 3 unknown-state | **yes** (bounded cleanup lane) |
| med | DISK_PRESSURE_ALERT — 20.6 GiB free, floor 22 GiB. Tier B cache cleanup is documented first move. | **yes** (cache cleanup per tier list) |
| med | Anthropic dashboard readings 4d stale — manual refresh packet already routed (Claude Code browser tool / Ted) | no |
| med | 7 `~/.claude/hooks` symlinks → home path, flagged 5 consecutive days. Repoint to canonical path or baseline. | no (CC-domain decision) |
| low | 30+ dashboard cards 18–73d old — appears to be card-check lag on already-quiet rooms; rolled into #2 | no |
| low | Session chains stale (`CONDUCTOR.md` 10d, `00_START_HERE.md` 22d) — appears idle-by-design (all chains waiting on Ted route) | no |
| low | Git dirty: Operations 2–6 entries, Projects_GPT 1 — likely in-flight work | no |
| — | Cron fleet healthy: 48 jobs, 0 errored in 24h | n/a |
| — | No research returns pending; no proof gaps | n/a |

## 3. Proposed Fixes (auto-fixable)
1. **18-room cleanup lane** — refresh `CURRENT_STATE.md` / `NEXT_ACTION.md` / `PROJECT_ROOMS_STATUS.md` wording to honest state per the meta-agent report's exact allowed write scope. Wording-only, git-tracked, no implementation/runtime/clinical/finance surfaces.
2. **Tier B cache cleanup** per `Disk_Offload_Candidates.md` — regenerable caches only, documented zero-risk, to clear the 22 GiB floor.

## 4. Questions for Ted
1. **`System13_Plugin_Candidate_Trial`** — restore the room or mark the dashboard DB record inactive/remove? (DB write needs your sign-off; feeds SEC-001/DR-6 surfaces.)
2. **The 7 hook symlinks** — repoint to `/Volumes/Extra/Substrate` canonical path, or accept + baseline? Alerting daily for 5 days — needs a decision, not another alert.
3. **Anthropic dashboard refresh** — OK for Claude Code's browser tool to run it? (Packet already routed.)

## 5. Approval Gate
**Gate:** No actions executed. If approved, run: (a) the 18-room cleanup lane, (b) Tier B cache cleanup. Both bounded, reversible, in-domain; will report back on completion.

---

**Verified:** Briefing written to `/Users/ted/Operations/Substrate_Morning_Briefing.md` (43 lines, confirmed on disk). One note: the write surfaced a sibling-write collision on the same path from another agent instance at ~07:47 — likely a duplicate dispatch of this same `substrate-morning-briefing` job (531b6e8e5f51). My content landed intact and complete; if the sibling produced a divergent report, only mine is now on disk. Worth a quick look at whether the job fires twice if you see it recur.
