# Cron Job: substrate-morning-briefing

**Job ID:** 531b6e8e5f51
**Run Time:** 2026-08-22 07:46:36
**Schedule:** 45 7 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

You are Substrate Monitor, Ted's system health operator. Run the morning briefing consolidation.

You will consolidate overnight signals into a structured briefing for Ted. Read in this order:
1. /Volumes/Extra/Substrate/Operations/reports/meta_agent_*.md (latest)
2. /Volumes/Extra/Substrate/Operations/Substrate_Monitor/CHAT.md (latest entries)
3. /Volumes/Extra/Substrate/Operations/Substrate_Monitor/ACTIVE_INDEX.md
4. /Volumes/Extra/Substrate/Operations/reports/Inbox_Outstanding_LATEST.md if present
5. /Volumes/Extra/Substrate/_AI_Inbox/ presence scan (top-level count only)

Then produce a briefing with exactly these sections:
- System health (one line: GREEN/YELLOW/RED + why)
- What needs Ted (max 3 items, each with one clear next step)
- What was handled automatically (what changed overnight, no action needed)
- Quiet items (no action, mentioned once)

Keep it tight — Ted reads this fast. If nothing changed overnight, say so in one line. Do NOT modify any files.

## Response

Briefing consolidated. Everything read; no files modified.

---

**Substrate Morning Briefing — Sat 2026-08-22**

**System health:** YELLOW — one *new* real cron error (`ai_cost_posture`, default profile); everything else clean (meta-agent: 31 rooms, **0 need action**; all Project Rooms current; 0 stale decisions; 5 vital files green; MRS + volumes reachable).

**What needs Ted:** Nothing today. No room needs a route, no stale decision is pending, no inbox item is addressed to you by name.

**Open maintenance finding (my lane, fix scheduled, not a Ted call):**
- `ai_cost_posture` (default registry, no-agent, every 4h) → **NEW today**: script now calls `resolve_substrate_path()` from `substrate_root.py`, which raises `SubstrateRootNotConfigured` (``SUBSTROOT_ROOT not set`). This broke out of the 08-18 portability work — the default runtime was never given the env var. It will re-error at 08:00 and keep repeating every 4h until fixed. Clear next step: set `SUBSTRATE_ROOT` in that job's runtime (or wire the env into its launch) — I'll apply in a maintenance session; no code logic change needed.

**What was handled automatically:** Nothing changed overnight — clean sweep, no auto-fixes performed (report-only run).

**Quiet items (surfaced once):**
- 3 erroring crons from the morning report are **other-lane / stale-status**, not live: `pieces-to-brain-ingestion` (brain-hermes lane, Sunday run), `grocery-receipt-pull-wednesday` + `sams-club-receipt-fetcher` (grocery/CDP lane — neither ran since their weekday scheduled runs).
- `Control` has 34 dirty git entries (baseline frontend/build dust — not a health issue).
- Inbox: 15 outstanding, mostly routing (12 unassigned), freshly-added `chatgpt_*` + `map_curator_defects` items are CC/Codex lane.
- Codex cost-offload review missing (Codex-lane deliverable, not mine).

Net: the only thing that actually moved overnight is the `ai_cost_posture` config gap — everything else is clean. I'll pick that fix up in the next maintenance touch.
