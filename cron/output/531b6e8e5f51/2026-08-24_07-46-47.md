# Cron Job: substrate-morning-briefing

**Job ID:** 531b6e8e5f51
**Run Time:** 2026-08-24 07:46:47
**Schedule:** 45 7 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

You are Substrate Monitor, Ted's system health operator. Run the morning briefing consolidation.

You will consolidate overnight signals into a structured briefing for Ted. Read in this order:
1. /Volumes/Extra/Substrate/Operations/reports/meta_agent_*.md (latest)
2. /Volumes/Extra/Substrate/Operations/Substrate_Monitor/CHAT.md (latest entries)
3. /Volumes/Extra/Substrate/Operations/Substrate_Monitor/ACTIVE_INDEX.md
4. /Volumes/Extra/Substrate/Operations/reports/Inbox_Outstanding_LATEST.md if present
5. /Volumes/Extra/Substrate/_AI_Inbox/ presence scan (top-level count only)

Then produce a briefing with exactly these sections:
- System health (one line: GREEN/YELLOW/RED + why)
- What needs Ted (max 3 items, each with one clear next step)
- What was handled automatically (what changed overnight, no action needed)
- Quiet items (no action, mentioned once)

Keep it tight — Ted reads this fast. If nothing changed overnight, say so in one line. Do NOT modify any files.

## Response

# Substrate Daily Briefing — 2026-08-24

**(Consolidated 07:45 EDT from meta-agent 00:30, brain-hermes cron pulse 06:21, live bridge audit)**

## System health — **YELLOW**
Two real overnight findings: **11 substrate-hermes cron jobs erroring** at import (`SUBSTRATE_ROOT` unset in cron runtime — my core domain), and a **HARD bridge-audit alert** (74× 401 `unauth` from localhost — likely local tooling noise, not external abuse). Meta-agent itself is clean (0 need action, 31 rooms). No crashes, no disk pressure.

## What needs you (max 3)
1. **SUBSTRATE_ROOT regression — 11 of my cron jobs failing** (`hermes-coordinator-health`, `cron-health-receipt`, `proactive-repair`, `bridge-health-check`, `mrs-freshness-sweep`, `memory-index-curated`, `symlink-home-path-sweep`, `cc_inbound_rollup`, `changes_log_size_rotate`, `codex_review_generator`, `yield_refresh`) — all `SubstrateRootNotConfigured: SUBSTRATE_ROOT is required` at import. Same bug class as the resolved #745 but a **new instance** (cron runtime env lost/never had the var). Home hard-fail finding from brain-hermes, not a duplicate — verify + restore the env (I was told not to write files this run).
   → **Next step:** on next interactive session, restore `SUBSTRATE_ROOT` in the substrate-hermes cron env (cron env config / launchd plist / wrapper), don't blind-fix.
2. **Bridge HARD alert — auth_failure_storm 74×** (`/api/materials/compare` POST, `unauth`, 127.0.0.1 loalhost curl, 401). Checked the last 20 min live: consistent `curl/8.7.1` localhost bursts = a worker/probe hitting the bridge with no valid key, **not** external abuse (no foreign IPs, no pattern beyond that one endpoint).
   → **Next step:** identify which local agent/script is polling `/api/materials/compare` unauthenticated (likely a piececheck loop) and fix or give it a token. No kill-switch warranted.
3. **Grocery weekly budget 403 (standing rule)** — `grocery-receipt-pull-wednesday` returned HTTP 403 weekly budget-limit (shared OpenRouter key). Separate from the SUBSTRATE_ROOT set (different cause).
   → **Next step:** budget raise or per-jab key — your call.

*(Also noted but lower-tier: `grocery-receipt-pull-sunday` RuntimeError — output truncated; not part of the root-cause cluster.)*

## Handled automatically (overnight, no action)
- **Meta-agent sweep 00:30** — 31 rooms, **0 need action**, 3 worth a quick look (all sub-10d), report-only run (0 writes). Cleanest morning in weeks.
- **Inbox audit** — regenerated 11:22; 5 outstanding, all unassigned (incl. the 2 new hard packets below). Nothing consumed-and-missed.

## Quiet (mentioned once, no action)
- Rooms approaching stale (not actionable): QuickSave 9d, Research_Coordination 8d, Hermes_Operator_Integration 8d — refresh when they cross 10d, not now.
- Audit Request queue: true-empty. No claude-validator response pending.

**Bottom line:** the fleet is largely healthy and clean except the **SUBSTRATE_ROOT cron-env regression** (item 1) — that's the one real system risk; the bridge 401s are noise to confirm, not a live threat.
