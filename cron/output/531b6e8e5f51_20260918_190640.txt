Cronjob Response: substrate-morning-briefing
(job_id: 531b6e8e5f51)
-------------

**Substrate briefing — 24h to Fri 2026-09-18 19:00 EDT — overall: YELLOW**

**Period and picture:** 391 fleet runs (20 failed), 274 report-runs inventoried, 58 substantive. The day's loud cluster — `symlink-home-path-sweep` failing hourly (17×) on `/Users/ted/Operations` — was root-caused and closed by Codex today (work item #1775): the dir held one 557-byte agent scratch script, both are gone, the alert file is archived, and the sweep has been green since 17:23. Two genuinely open issues remain: the Reunion family backup (down 4+ days) and a flaky Gemini-over-CDP capture lane. Everything else flagged is stale Sunday state or a designed alert.

**What needs Ted**
1. **Reunion family-file backup is still down** — newest snapshot 2026-09-14 03:00 (~110h). No launchd/cron producer exists on this Mac, so only the app can restart it. Next step: open Reunion 14 on the machine that runs it and re-run/re-enable its daily copy; my 9am watchdog clears itself once a snapshot lands. Note: both reminders already fired (Sep 17+18, that job is now completed), and the planner record for this fix carries timing **2027-09-14** — as written it won't resurface for a year, so it needs a one-word correction.
2. **Two low-priority fleet questions still parked on you** (ACTIVE_INDEX "⏳ Ted"): the fleet census (plan says 92 jobs, live is 108 — pick the number so docs stop drifting) and whether to widen zai-plan-economics now or later. One-line answers are enough.
3. No other decision is needed today.

**What will be handled next**
- **Gemini capture lane** — `apple-signal-scan-daily` is ~50% failing since Sep 14 (alternating DevTools-blocked / "Gem contract breach") and `gemini-unified-master-digest-daily` failed today after succeeding yesterday; both ride the same Gem-over-CDP path, so this is one flaky lane, not two outages. Next: retry-on-contract-breach + settle-time; owner substrate-hermes/CC; next fires 06:45 / 08:20.
- **My own preflight lies**: `~/.hermes/scripts/session_state_preflight.py` reads the *default* profile's `cron/jobs.json` (19 jobs, clean) instead of the running profile's (108 jobs, 5 in error), so its "Cron health (24h): GREEN" is a false negative. Owner substrate-hermes: make the path profile-aware.
- **Grocery/Sams Sunday fetchers** stay in stale `error` until their next fire Sun 09-20 09:30 (error is from the 09-13 CDP-down; WI #1641 acted) — Sunday proves it.
- **3 stale Project Rooms** (Canon_Reshape 33d, Clinical_GPT_Development 34d, Hermes_Operator_Integration 18d) — today's autopickup reported no_change; state refresh is the next step.
- **Possible stale duplicate**: #1768 (cold-reader `:free`-suffix defect) is still `pending` while the same defect's #1780 is `acted` with the classifier fix live — reconcile/annotate rather than rework.
- **Inbox**: 17 outstanding; 6 owed to Claude Code (incl. 3 map-curator defect reports) and 3 claude_code consumption stamps claim files that don't exist (false completions) — owners to re-open/verify.

**What was handled automatically**
- `symlink-home-path-sweep` self-cleared end-to-end (scratch script relocated to Backups, banned dir removed, alert archived, 2 clean runs, status ok) — no action needed.
- `ai-cost-dashboard-freshness-check` cleared to **ok** at 09:00 (was a 5-day designed alert).
- Dead-path scan: 6 actionable refs across 3 docs, **0 genuinely new** today; WI 1776 dead-path repair closed out same day.
- Routine surfaces all clean: audit-router 0 requests, feed health 0 attention, icon gallery PASS, brain ingest OK (136 notes/7d, newest 15h), DeepSeek balance $32.00 (burn $0.21/day), deepseek/mrs sweeps no-op.

**Quiet or archived**
- "Flagged" but report-clean: `feed_health_watch`, `icon-gallery-freshness-watchdog`, `audit-router`; `inbox_aging`'s single item is the 8-day-old coherence-kickoff note.
- This morning's daily report listed 6 erroring jobs — it was generated at 07:00, before the 09:00 clears and the 17:00 sweep fix.
- Known doc drift (already tracked as SUBSTRATE_HERMES_PLAN item 2): my SOUL still lists `overnight-consolidated-morning` (deliberately removed earlier, with a CHANGES_LOG record) and calls two now-script jobs "agent".

**Evidence**
`Operations/reports/SUBSTRATE_PERIODIC_DIGEST_LATEST.md` (274 runs, 30 flagged) · `reports/meta_agent_2026-09-18.md` · `Operations/CHANGES_LOG.md` (2026-09-18 entries, WI #1775/#1779/#1780) · `Operations/planner/requests/har_20260916T150954Z_fix-reunion-daily-family-file-backup_446f2a83.json` + iCloud `Mom Files/Reunion_Backups` (newest Sep 14, no `com.reunion.dailycopy` service loaded) · `~/.hermes/profiles/substrate-hermes/cron/jobs.json` (108 jobs; 5 error-state) · `reports/Ted_Decisions_LATEST.md`, `reports/Inbox_Outstanding_LATEST.md`, `reports/Dead_Path_New_Documents_LATEST.md`, `reports/Scheduled_Followups_LATEST.md`

To stop or manage this job, send me a new message (e.g. "stop reminder substrate-morning-briefing").