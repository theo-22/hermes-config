# Cron Job: apple-signal-digest-daily

**Job ID:** 70dba0823a1e
**Run Time:** 2026-09-25 07:00:22
**Schedule:** 0 7 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. [SILENT] is a literal ASCII control token — never translate or rephrase it, whatever language the rest of your answer uses. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more. FAILURE: If a delegated child fails and this cron run must be recorded as failed, put [CRON_FAILURE] on the first line by itself, then explain the child failure on following lines. RECURSION: This is a run of an EXISTING scheduled job — execute the task now. NEVER create or update a cron job because of recurring or future-schedule language in the task prompt below; treat phrasing like "each Monday" or "every day at 9" as context for this run, not as a request to schedule another job.]

New items were captured by the apple_signal_scan.py cron in Operations/state/apple_signal_scan.jsonl (Ted's macOS 27 / Siri / Apple Intelligence rollout monitor, sourced from Gemini's real web-search-grounded Signal Scan gem). Read the newest entries in that file (each line is JSON: date, topic, headline, source_class, confidence, url, relevance) — read only the ones appended since your last run, or if unsure, the last 10 lines. Write ONE short, plain-language digest for Ted: what's genuinely new, interesting, or useful about the macOS/iOS 27 Apple Intelligence and Siri rollout — skip anything trivial or already-known. Prioritize anything that would actually help him (capabilities coming online, real limitations discovered, developer-relevant findings) over generic news. Label each item's confidence (the source_class/confidence fields tell you this) so he knows what's verified vs. rumor/chatter. Keep it tight — a few bullets, not a wall of text. Include the real URLs. If nothing in the new entries is actually worth surfacing, say so in one line instead of padding.

## Run Context
## MONITOR CHANGE DETECTED

The monitored source's output changed since the last run.

### Diff (previous → current)

```diff
--- previous
+++ current
@@ -1 +1 @@
-LINES:29
+LINES:33
```

### Current output

```
LINES:33
```

## Response

**Apple Signal Scan — 4 new items (2026-09-25)**

**High confidence — App Intents registration failures (developer-relevant)**
• Custom `AppEntity` parameters in trigger phrases cause Shortcuts to drop registration entirely — fix: omit custom entities from phrases, restore missing AppShortcuts. [Origin](https://origin-devforums.apple.com/forums/thread/707851)
• Xcode toolchain selection + parameter query bugs hide App Intents from the system — check toolchain alignment if intents vanish after build. [Reddit](https://www.reddit.com/r/iOSProgramming/comments/v8w2ls/appintent_not_appearing/)

**Medium confidence — Siri phrase matching drift**
• Dynamic Siri matching behavior changes how AppShortcuts phrase binding works — reported phrase-matching failures with custom App Intents under the Siri AI layer. [Dev Forums](https://developer.apple.com/forums/thread/837204)

**Medium confidence — practical fix**
• Spotlight index rebuild workaround for missing app items — if Siri/Spotlight entities go missing post-update, a manual index rebuild addresses it. [MacMost](https://macmost.com/apple-has-a-spotlight-problem-and-needs-to-fix-it.html)

Bottom line: App Intents registration is fragile under iOS 27 — if you're building Shortcuts automation, verify intents register after every Xcode/toolchain change. Spotlight rebuild is the only user-side fix for entity gaps.
