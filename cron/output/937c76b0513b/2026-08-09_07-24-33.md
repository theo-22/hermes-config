# Cron Job: work-queue-weekly-pass

**Job ID:** 937c76b0513b
**Run Time:** 2026-08-09 07:24:33
**Schedule:** 15 7 * * 0

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Weekly work-item pass (repointed 2026-08-04 -- WORK_QUEUE.md is retired; work_items in system.db is now the one real record. Ted: "hard to catch all the do it later stop now stuff").

Query the live work queue: curl "http://localhost:5555/api/work-items?state=pending&actor=substrate_hermes" -- this returns everything pending that is NOT hard-gated away from you (owner is a soft "likely best fit" hint, only a required_owner match or blank means you're free to act; a required_owner set to someone else means skip it, it's not yours to force).

For each item returned where owner is "claude_code" or blank (this job's realistic buildable range, same scope as the old section A):
1. Read enough surrounding context (linked files, referenced code -- follow source_surface) to understand exactly what it's asking for.
2. Check LIVE STATE FIRST before building anything -- this codebase has a real, repeated pattern of items already being done and never marked closed. Grep/read the actual current file or run the actual current code before assuming the item is still open.
3. If it's already done: verify it live (run it, curl it, read the actual current file), then close it out for real: curl -X POST "http://localhost:5555/api/work-items/state" -H "Content-Type: application/json" -d '{"item_id": <id>, "state": "acted", "actor": "substrate_hermes"}' -- and add a note of what you checked by first reading the row's current notes and PATCHing an updated notes field via a fresh POST /api/work-items with the same source_surface if the API doesn't support notes-append directly, or by appending to the row's notes column with a short SQL UPDATE against system.db (append, do not overwrite the original context).
4. If it's genuinely open and buildable: build it, then LIVE-VERIFY the result (run the code, restart the relevant service if one exists e.g. `launchctl kickstart -k gui/$(id -u)/com.ted.control.mcp` for Control/mcp/server.py changes, test the actual behavior) before closing it via the same state-transition call. Never mark something closed on the basis of "the code looks right" alone.
5. If while working an item you discover it actually needs Ted's judgment, is blocked (permissions, a contested fact, missing infrastructure), or turns out bigger than "just build it" -- do NOT force it through. Set its required_owner to "ted" via a raw SQL UPDATE against system.db's work_items table (or ask Ted directly if you have a live channel) with a clear note explaining why. Being honest about what's blocked is the job, not a failure of it.
6. Commit each real code change with a clear message, in the correct git repo (each top-level Substrate directory -- Roles/, Planning/, Operations/, Concept_Graph/, etc. -- is its own repo; Control/ under /Users/ted is a separate repo from all of them).

Stop after working through what's realistically doable in one pass -- do not rush low-quality closures just to empty the list. If nothing returned is runnable this week (all genuinely blocked/ted-gated), say so plainly rather than inventing something to close.

Do not touch items where required_owner is already set to someone other than you -- those need Ted's input, a different actor, or an external condition, not another automated pass.

End with a short summary: how many items closed, how many reclassified (required_owner set), how many left untouched and why.

## Response

CHANGES_LOG committed (7b4eab53). The 2 remaining dirty files in Operations (`Claude_Handoff/sessions/...` and `Substrate_Status.md`) are the known preflight baseline dirty entries — not mine, not touching them. 

Pass complete. Final summary:

---

## Weekly Work-Item Pass — 2026-08-09

**Queried:** `GET /api/work-items?state=pending&actor=substrate_hermes` → **38 pending items returned** (none hard-gated by required_owner). Owner distribution: ted 24, codex 9, claude_code 2, orchestrator 2, Workspace Icon System 1.

### In-range items (owner `claude_code`/blank): 2 — both reclassified to Ted

**#268 — Turn Instrumentation into a real portable role** → `required_owner=ted`
- Live state checked: no `Roles/Instrumentation/` Home, no `ROLE_LAUNCH_REGISTRY.md` row, no draft apparatus anywhere (grep/find across Substrate + /Users/ted; no post-08-04 CHANGES_LOG work). Genuinely open.
- Not closable by an automated pass: (1) **contested authorization** — `_shared/CC_Instrumentation_Capability.md` (08-01) records Ted's explicit "CC-only for now… expand later" call, while the item quotes Ted 08-04 "likely should be a role"; (2) the item's own note says "CC may draft it but must not accept it," and the acceptance gate requires a **fresh non-CC actor session** doing real Instrumentation work — impossible to self-produce from cron; (3) the role model is in flux (08-08 Codex reframe: Scribe = callable service, not inhabitable role; CHANGES_LOG 08-09 flags the same question). Per pass rule 5, set `required_owner=ted` with a clear appended note rather than forcing a half-build.

**#296 — Non-CC actor acceptance of repaired scribe_check_existing** → `required_owner=ted`
- Live state checked: tool exists in `Control/mcp/server.py` (READ_ONLY annotations); bridge `/sse/scribe` → **401 auth-gated**; **no Hermes profile has role-scoped scribe MCP wired** (verified every profile config.yaml — only `model_id: scribe_v2` audio-config hits, no MCP server); no backend HTTP route. I'm a non-CC actor but cannot reach the tool without wiring a worker profile + role-scoping (its own real build per the 08-06 note) or a fresh ChatGPT-side session — and bridge auth is a protected surface I must not bypass.
- The gate itself is **contested**: the 08-08 Codex packet reframes Scribe as "a callable work/recording service… not an inhabitable role," and CHANGES_LOG 08-09 explicitly asks whether the fresh-session-acceptance procedure even applies to Scribe. Ted must rule on whether the gate still applies before any build.

Both updates done via raw SQL UPDATE on `system.db` (notes **appended**, originals preserved, `state_changed_at` stamped), verified by read-back and by the unfiltered API (`required_owner='ted'` confirmed). Both now correctly drop out of my actor-scoped pending query (intended hard-gate).

### Touched surface
- `Operations/CHANGES_LOG.md` entry added, committed `7b4eab53`.
- `Substrate/CHAT.md` updated with pass record + health summary.

### Totals
- **Closed: 0** (neither in-range item was done or buildable-to-close; both needed Ted's judgment, not another automated push)
- **Reclassified (required_owner=ted): 2** (#268, #296)
- **Left untouched: 36** — all owned by ted (24), codex (9), orchestrator (2), or Workspace Icon System (1); outside this pass's `claude_code`/blank buildable range and none hard-gated to me.

**Session health:** preflight YELLOW (transient Operations dirt — clean at closeout, auto-commit watcher handled), cron pulse silent except the known by-design symlink finding (CC surface, already surfaced), watchdog silent, no inbox items addressed to me, no claude-validator response pending.
