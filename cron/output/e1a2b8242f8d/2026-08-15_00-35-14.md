# Cron Job: meta-agent-sweep

**Job ID:** e1a2b8242f8d
**Run Time:** 2026-08-15 00:35:14
**Schedule:** 30 0 * * *

## Prompt

[IMPORTANT: The user has invoked the "meta-agent-pattern" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: meta-agent-pattern
description: Build and maintain meta-agents that monitor project health across multiple data sources (Pieces, session transcripts, file changes, cron outputs) and update Project Room state files automatically. Use when designing systems that maintain other agents or project contexts without human intervention.
version: 1.0.0
author: Hermes Agent
license: MIT
platforms: [macos, linux]
metadata:
  hermes:
    tags: [meta-agent, project-room-maintenance, automation, multi-source, cron]
    related_skills: [hermes-operations, pieces]
---

# Meta-Agent Pattern

Build agents that maintain other agents. A meta-agent reads from multiple data sources, compares actual activity against documented state, and updates project contexts automatically — so humans and AI sessions start with fresh context.

## When to Use

Trigger when:
- Project Rooms are drifting from actual activity
- You need to check multiple sources (Pieces, sessions, files, cron outputs) for project health
- You want to automate the "what changed since last session" workflow
- You're building a system that maintains other agents or project contexts

## Architecture

```
Data Sources → Meta-Agent → Project Room Updates
     ↓              ↓              ↓
Pieces digest   Compare      CURRENT_STATE.md
Session logs    actual vs     NEXT_ACTION.md
File changes    documented    Meta-report
Cron outputs    state
```

### Data Sources (in priority order)
1. **Pieces daily digest** — `~/Hermes/Working/pieces_continuity_YYYY-MM-DD.md`
2. **Session transcripts** — via `session_search` tool
3. **File changes** — git diff or mtime checks in Project Room directories
4. **Cron job outputs** — evaluation reports, review files

### Outputs
- Updated `CURRENT_STATE.md` and `NEXT_ACTION.md` per Project Room
- Daily meta-report at `~/Operations/reports/meta_agent_YYYY-MM-DD.md`
- Action log at `~/Operations/state/meta_agent_log.jsonl`

## Implementation Pattern

### 1. Script Structure
```python
# meta_agent_sweep.py
def main():
    # 1. Gather data sources
    pieces_digest = get_pieces_digest()
    sessions = get_recent_sessions(hours=24)
    
    # 2. Analyze each Project Room
    for room in get_project_rooms():
        analysis = analyze_project_room(room)
        update_project_room(room, analysis)
    
    # 3. Generate report
    report = generate_report(analyses, pieces_digest, sessions)
    write_report(report)
```

### 1a. Room discovery — canonical Home directory scan

The sweep discovers rooms by scanning five canonical Home directories for subdirectories that contain `CURRENT_STATE.md`:

```python
HOME_DIRS = [
    Path("/Volumes/Extra/Substrate/Commons"),
    Path("/Volumes/Extra/Substrate/Roles"),
    Path("/Volumes/Extra/Substrate/Hermes"),
    Path("/Volumes/Extra/Substrate/Brain"),
    Path("/Volumes/Extra/Substrate/InterfaceFlow"),
]

def get_project_rooms() -> list[Path]:
    rooms = []
    for home in HOME_DIRS:
        if not home.exists():
            continue
        for item in home.iterdir():
            if item.is_dir() and (item / "CURRENT_STATE.md").exists() and item.name not in ROOM_EXCLUDE:
                rooms.append(item)
    return sorted(rooms)
```

This replaced the old `~/Projects/` symlink scan (2026-07-11). Key changes from the old approach:
- **Discovery signal:** `CURRENT_STATE.md` presence (was `README.md` presence)
- **Search scope:** 5 canonical Home directories (was a single `~/Projects/` directory)
- **Excludes** generic names like `room` (staging artifact) that happen to have CURRENT_STATE.md
- **Symlinks no longer required:** rooms that never had a `~/Projects/` symlink (e.g., `model_testbench`) are automatically discovered

**ROOM_EXCLUDE:** set `{"room"}` to prevent staging artifacts (e.g., `InterfaceFlow/room/` — a pre-cutover staged room model) from being counted as Project Rooms. Add to this set if other non-room directories with CURRENT_STATE.md appear.

**Before migrating to Home scan:** The sweep used `BASE / "Projects"` which resolved through `~/Projects/` compatibility symlinks. That approach missed rooms that lacked symlinks and included staging artifacts. The canonical scan fixed both.

**Cron relevance evaluation (2026-07-11):** Ted asked whether the meta-agent-sweep is still relevant. Answer: yes — it's a safety net that catches room drift before it compounds. ~$0.30-0.60/mo for daily off-peak runs. Consistently finds 0-1 flagged rooms, meaning rooms are healthy (not that the cron is useless).

### 2. Staleness Detection
- **Active**: Updated within 14 days (and lifecycle is "Active")
- **Review**: No update in 14-30 days (and lifecycle is "Active") — flag for human review
- **Stale**: No update in 30+ days (and lifecycle is "Active") — mark as stale
- **Maintenance rooms**: NEVER flagged for staleness — they are quiet by design

### 3. Update Rules (Principles, Not Hard Rules)
- Maintenance rooms are quiet by design — never flag for staleness
- If room hasn't been touched in 30+ days AND lifecycle is "Active" AND no live strand exists → mark stale
- If git shows recent activity but state file is old → the room may still be active; check content before changing
- Never auto-archive — that requires human decision
- Always record reasoning in the action log

### 4. Cron Schedule

Run outside DeepSeek peak windows (W1: 9pm-midnight, W2: 2am-6am EDT — both 2x pricing). Safe window: 6am-9pm EDT. No-agent scripts ($0) can run any time:
```
30 0 * * * meta_agent_sweep.py    # 12:30am — between W1 and W2, off-peak
```

## Key Principles (from Nate B Jones video — 2026-06-16)

1. **Architecture over tools** — Works with any source, not tied to one tool
2. **Principles over rules** — "If stale and no live strand, mark stale" not hard-coded thresholds
3. **Agent builds = agent maintains** — Meta-agent records reasoning so future runs understand past decisions
4. **Infrastructure, not tool** — Other agents (Claude, Codex, Hermes) can read the updated Project Rooms

## Communication Conventions (learned 2026-06-16)

- "Pieces" = Copilot chat assistant (not the system)
- "Scroll to bottom" = read full conversation transcript
- "The answer is there" = re-read the transcript more carefully
- When Pieces is still answering, wait then re-read
- File-based relay is NOT high friction — it's a reasonable workflow

## Model Picker Integration

The meta-agent produces a report; the model picker routes actions:
- **Routine cleanup** → `deepseek-v4-flash` via `deepseek` provider (direct, not OpenRouter — avoids middleman markup and peak pricing)
- **Synthesis/reasoning** → `deepseek-v4-flash` (adequate for structured summarization and grouping)
- **Quality evaluation** → `claude-haiku-4-5`
- **Authority decisions** → escalate to medium/heavy

**Model lifecycle notice (2026-07-04):** All agent-based crons now use `deepseek-v4-flash` via the `deepseek` provider. Do NOT pin to `openrouter/free` — it was the old routing path before DeepSeek direct was configured.

**DeepSeek peak windows (2026-07-04):**
- Window 1: 9pm–midnight EDT (2x)
- Window 2: 2am–6am EDT (2x)
- Agent-based crons must be scheduled outside both windows. No-agent scripts ($0) are unaffected.
- See Brain (search "DeepSeek peak pricing hours") or `cron-job-health-audit` skill pitfall 14 for full detail.

## Report-Only by Default (Codex refinement)

The meta-agent sweep runs report-only by default. It does NOT update Project Room files unless `--write-check-stamp` is explicitly passed. This prevents making rooms look fresh just by checking them.

## Content-Aware Analysis (Codex refinement)

The meta-agent reads actual file content, not just file mtime:
- `Room state last updated:` field
- `Card checked:` field
- `Lifecycle state:` field
- `Status:` field
- `Blocked:` field

This is more accurate than mtime alone.

## Stale-Flag Triage

When the sweep report flags rooms as "Needs Action" or "Worth A Quick Look", not every flagged room needs a lifecycle change. Determine the right response:

1. Read the room's `CURRENT_STATE.md` — check the Lifecycle state and the `Card checked:` / `Room state last updated:` dates
2. **"Quiet until trigger" rooms** — intentionally dormant, but the meta-agent **will re-flag them** at the 30-day threshold regardless of lifecycle. If they're >30 days old, they need a **date refresh** (not a lifecycle change). See "Quiet Room Refresh Pass" below.
3. **"Active" rooms waiting on Ted routing** are not stale — no action unless the "Card checked" date is outdated
   - **Exception: when Ted explicitly routes a cleanup pass** and meta-agent flagged "Worth A Quick Look" rooms have `Card checked` >7 days stale, bump the date. Do NOT change lifecycle or content — just `Card checked:`, `Room state last updated:`, meta-agent check stamp, and the dashboard row. This prevents them from re-flagging on the next sweep. (Real case 2026-07-08: Project_Interface_Workspace_Flow and Research_Coordination, both Active at 10 days, bumped by Ted's route.)
4. **"Settled / historical" rooms** — already handled; verify the dashboard row agrees
5. **Only change lifecycle** when the room's lifecycle state in CURRENT_STATE.md disagrees with its row in `PROJECT_ROOMS_STATUS.md`

See `references/stale-flag-triage-pattern-2026-07-04.md` for the full decision tree and a worked example.

## Pitfalls

- **Don't auto-archive** — marking stale is fine, archiving requires human decision
- **Don't over-update** — if state file is recent, don't touch it
- **Record reasoning** — every update should have a log entry explaining why
- **Respect room-local authority** — if CURRENT_STATE.md says "Active" but mtime is old, check git before changing
- **Handle errors gracefully** — if Pieces digest is missing, skip that source; don't fail the whole sweep
- **Wrapper scripts pinning absolute paths die silently when surfaces move.** A cron wrapper that checks `if not GENERATOR.exists(): sys.exit(1)` and otherwise subprocess-runs an absolute path will fail EVERY run with a bare exit 1 and no stderr once that path disappears — no model error, no traceback, no output, just a 177-230-byte "Script exited with code 1" run file that looks identical every day. Real case (2026-08-11): closure-index-generator's wrapper pointed at `/Users/ted/Projects_GPT/Audit/scripts/closure_index_generator.py`, the surface was reorganized to `/Volumes/Extra/Substrate/Audit/` on Jul 31, and the job failed silently from Jul 31 through Aug 11 (~12 days) until cron pulse surfaced it. Fix pattern: repoint wrapper to the canonical new path (`/Volumes/Extra/Substrate/Audit/scripts/closure_index_generator.py`), sync source → profile runtime copy, md5 both, run the PROFILE copy (what cron actually runs) to prove exit 0 + fresh output, then the next scheduled run clears the pulse error. Detection beyond pulse: compare a stale derived surface's "Last built" against run-file timestamps — CLOSURE_INDEX.md was still being refreshed Aug 10 by a different invocation while the cron job itself was failing, so "output looks fresh" is not proof the cron works.
- **Don't use Pieces QGPT API** — it's broken on PiecesOS 12.4.1 (500 errors). Use read-only tools or file-based relay.
- **Script sync after any actor edits a cron script:** The source `~/.hermes/scripts/` and profile runtime `~/.hermes/profiles/<profile>/scripts/` can diverge. After any actor edits a cron script, compare both copies. The profile dir is what cron actually runs — source changes alone don't take effect. Check both when debugging cron errors. Known real case (2026-07-04): Claude edited meta_agent_sweep.py source to a 193-byte wrapper but the profile still ran the old 22KB version, and 4 scripts were missing from the profile entirely.
  **Three-location layout (meta_agent_sweep.py, verified 2026-08-02):** `~/.hermes/scripts/meta_agent_sweep.py` is a ~200-byte wrapper that `runpy`s the canonical script at `/Volumes/Extra/Substrate/Operations/scripts/meta_agent_sweep.py`. The profile runtime copy at `~/.hermes/profiles/<profile>/scripts/meta_agent_sweep.py` is a full standalone copy — that is what cron runs. Sync path: canonical `Operations/scripts/` → profile runtime copy, then `md5` both and re-run to verify. Detection beyond md5: read the generated report's own output for stale path strings (e.g. report line `Full log: Projects_GPT/Audit/RUN_LOG.md` while canonical points at `Substrate/Audit/RUN_LOG.md`) — content evidence proves which copy ran. Real case (2026-08-02): canonical updated Jul 31 to the new path layout; profile copy was still the Jul 22 version with old paths; report generated with the old `Projects_GPT/Audit` log path; sync + md5 match + clean re-run fixed it.
- **Stale pre-fix cron errors:** A cron pulse/watchdog error can outlive the fix. Pulse reports the LAST run's exit; for infrequent schedules (weekly, monthly) that run may predate the fix by days. Before concluding a fix failed, compare the job's last run timestamp against the fix's file mtime — and expect the error to persist until the job's next scheduled run (daily clears next day; weekly e.g. shopping-guru Wed holds until Wed; monthly e.g. changes-log-dedup on the 1st holds until the 1st). Diagnose, don't re-fix. Real case (2026-08-02): role-workspace-access-check (daily 07:15, last run pre-fix) and shopping-guru-weekly-crossref (Wed 09:00, last run Jul 29, fix Aug 1) both showed pulse errors while the scripts were verified in sync (md5 matched).
  See `references/script-sync-and-stale-cron-errors-2026-08-02.md` for the full diagnostic + fix recipes.
- **Cross-profile cron error triage (whose output is where):** A cron error on a job that is NOT substrate-hermes's own (e.g. `brain_ingest_health` in the advisor profile) will NOT appear in `~/hermes-public-config/cron/output/` — per-profile job output lives at `~/.hermes/profiles/<profile>/cron/output/<job_id>/`. Triage order: (1) map name→job id via `~/.hermes/profiles/<profile>/cron/jobs.json` — exact shape is `{"jobs": [...], "updated_at": ...}`, so iterate `d["jobs"]`, NOT the top-level dict (naive `for j in json.load(...)` throws `AttributeError: 'list' object has no attribute 'get'`); (2) read the latest run file in that profile's output dir for the full stderr traceback; (3) classify transient vs persistent by comparing prior run files — OK/clean history back several days means transient, repeated same error means persistent. **"silent (empty output)" is a distinct run status from OK** — a job that ran silently for days then started failing changed behavior; do not treat the silent streak as proof of health. When a failure says a dependency is "unreachable", verify the dependency LIVE from your own context before classifying (e.g. call the MCP/brain stats tool); if it responds, the failure was time-window-specific, not an outage, and the next scheduled run confirms self-clear. A single `sqlite3.OperationalError: database is locked` on the shared `Control/backend/system.db` is a known contention class (tracked by `system_db_contention_monitor.py`): surface it, let the next scheduled run confirm self-clear, and do not fix another actor's job. Worked example: `references/cross-profile-cron-error-triage-2026-08-04.md`.
  **Same-window provider-stall cluster (real case 2026-08-08):** When 2+ agent-based jobs fail within minutes of each other with `RuntimeError: Provider has been unresponsive (no response received) for N consecutive stale attempts`, treat it as ONE provider-outage window, not N independent failures — even across profiles (system + brain-hermes both failed 06:55–06:59 EDT on 08-08, all on `deepseek-v4-flash`). Verify the provider LIVE (e.g. `curl -s -o /dev/null -w "%{http_code}" https://api.deepseek.com/v1/models` — 401 auth-gated = reachable and answering); if reachable, classify transient and let next scheduled runs self-clear. Distinguish from persistent model-drift errors (those say "global inference config drifted", never self-clear). Also note: the pre-run script output may show the job's real work succeeded (e.g. Brain Daily Dump's SQL backup completed) — the failure is the LLM reporting step, so the data-side effect exists even though the cron is marked FAILED.
- **EINTR on directory iteration = transient (real case 2026-08-14):** `InterruptedError: [Errno 4] Interrupted system call: '<dir>'` inside a `Path.iterdir()` loop is a signal-interrupted syscall, not a path defect — the directory can be a symlink to canonical and resolve fine. Classification test: N prior runs clean/silent → transient; surface it, expect self-clear next scheduled run, do NOT re-point or rewrite the script. Also note the script may legitimately live only in `~/.hermes/scripts/` + profile runtime with no Operations/scripts source — that is a different layout, not a sync defect.
- **Zero-byte `.db` files are AI probe decoys (real case 2026-08-14):** `stray-db-scan-weekly` finds 0-byte `system.db`-style files created by an actor calling `sqlite3.connect()` on a wrong path (silent file creation, no error). Auto-fix is bounded and safe: grep all script dirs for live references to the exact path; if none, `mv` to `~/.Trash/<name>-decoy-YYYYMMDD` (never `rm`). The real shared DB is `Control/backend/system.db`; a decoy under `Operations/state/` is a probing artifact.
- **Verify actor service-death claims live before reporting (real case 2026-08-14):** CC notifications said PiecesOS "gone"/died during boot (09:23 UTC); a live `pgrep -fl Pieces` showed 2 processes running (`Pieces.app` + `Pieces OS.app`). An actor's "service is down" notification is a snapshot — check the process table yourself before echoing the claim or acting on it; the two can disagree because the observer's window differed or a restart happened.
- **Dual-location status lists (ACTIVE_INDEX.md gotcha):** The same room-status summary appears in BOTH the Active Work Items table row and the System Status section. When patching one occurrence, include the section-unique line prefix (`||| **Meta-agent sweeps** |` vs `- **36 Project Rooms** —`); the bare shared substring fails with "Found 2 matches". Do not issue two same-file patches in one parallel batch — they race, and the fuzzy matcher can see the unchanged copy in the other location. After any ACTIVE_INDEX.md table-row patch, verify the leading pipe count matches neighboring rows (`grep -n '^||||' ACTIVE_INDEX.md`); a row with one extra `|` (e.g. 7 pipes among 6-pipe rows) writes fine but breaks the table render — real case 2026-08-14.
- **computer_use not available in chat** — only works in TUI/desktop sessions
- **Symlinks from ~/.hermes/scripts/ to ~/Operations/scripts/ ARE accepted** — the cron gateway follows symlinks. This was relaxed; earlier versions may have rejected them.
- **Deliver setting:** Use `deliver: "origin"` not `deliver: "local"` — "local" causes false errors on any stdout output.
- **Script exit codes:** Always return 0 on successful completion. Encode status in the output file, not the exit code (YELLOW=1, RED=2 were flagged as errors).
- **Recreating deleted cron jobs:** If a cron job's script was deleted in a reorganization, check the backup at `/Users/ted/Desktop/hermes-merge-backup-20260612-094507/live-home/scripts/` for the original, then recreate the cron job with the restored script.
- **Script location for profile cron jobs:** Cron jobs in a profile (e.g., substrate-hermes) look for scripts in `~/.hermes/profiles/<profile>/scripts/` NOT `~/.hermes/scripts/`. When creating a new script for a profile cron job, copy it to the profile's scripts directory, or use a symlink. We tested: copying works; symlinks also work from the main scripts dir.
- **Consolidated no-agent overnight job pattern:** The 4 Codex overnight jobs (proof-gap, drift-scout, research-return, threads-and-rooms) were INACTIVE but could be reactivated. Instead, we created one consolidated `overnight_consolidated_morning.py` that runs daily 4am as a no-agent cron, checking: room drift (D1-D5), proof gaps, research returns, and thread/room status. This replaces 4 paid Codex runs with 1 free local run.
- **Audit triage cron recreation:** The `audit-request-daily-triage` cron was lost in hermes-merge (script deleted). We restored `audit_triage_daily.py` from backup and recreated the cron (daily 6am, agent-based). It processes the Audit_Request_Queue, deduplicates, runs mechanical checks (JSON parse, schema validation), and produces a morning summary.
  **Verifying a "0 processed" result (real case 2026-08-05):** When the triage summary reports 0 across all categories, confirm it is a true empty, not a miss. The script consumes ONLY `*.json` files in `Audit_Request_Queue/pending/` (plus dedupe against the queue). The `Audit_Alert_High_Signal_*.md` files in `_AI_Inbox/` (from Claude stop hooks, `status: pending-review`) are a SEPARATE review flow — the triage does NOT consume them, so their presence does not contradict a 0 result. Check `ls -la <queue>/pending/` for JSON requests and confirm `Summary_Latest.md` regenerated at run time. Surface the pending `.md` alerts as a routing decision (audit case vs archive) in the report rather than folding them into the queue count. Same-file-repeated pattern worth calling out: two alerts flagging the same path within hours (e.g. `Control/mcp/server.py` twice in ~5h) is a higher-signal routing question than a one-off alert. See `references/audit-triage-verification-2026-08-05.md` for the full worked example (true-empty check, jobs.json shape, unreachable-dependency classification).
  **Routing escalation when a decision stays open (real case 2026-08-07):** If a surfaced audit-alert routing decision gets no disposition AND the pattern intensifies (same file flagged again — 2 → 4 stop-hook alerts on `Control/mcp/server.py` in ~32h over 3 days), don't re-surface it a third time — operationalize the routing. Create a well-formed request JSON in `Audit_Request_Queue/pending/` with `handler_recommendation: "audit_gpt"` (triage maps this to `audit_candidate` → moved to `dispatched/`), `mechanical_checks_requested: []`, and a unique `dedupe_key`. Then re-run `audit_triage_daily.py` to PROVE the full pipeline: parse → dedupe → classify → move → `Summary_Latest.md` shows "Dispatch Ready: N". The v0 report-only trial means nothing dispatches without Ted/Codex approval, so this is bounded and safe — it moves the decision forward without acting unilaterally. Before writing the audit question, check live evidence (file mtime vs alert timestamps, git log on the path) to refine it — e.g. intentional feature commits mean the question is consistency/coverage across sessions, not churn/corruption. Leave the alert `.md` files in `_AI_Inbox/` as evidence (reference them via `change_refs`). See `references/audit-alert-routing-escalation-2026-08-07.md` for the full worked example including the request schema, triage script mechanics, and the verification-harness tempdir pattern.
  **Post-routing candidate placement verification (real case 2026-08-08):** On later triage runs, verify the candidate's ACTUAL queue directory — do not trust the CHAT.md entry from the session that created it. Real case: the 08-07 candidate `audit-alert-control-mcp-server-20260806.json` was recorded as "now in dispatched/" but was found in `done/` the next day (undocumented move; git commit a39fd10d showed it added under `done/`). `done/` is the terminal dir for `mechanical_pass`; `audit_candidate` belongs in `dispatched/` per `move_to_outcome` and README. Restore is bounded and safe: `mv` back to `dispatched/`, keep `status: audit_candidate`, stamp `_restored_at`/`_restore_note` in the JSON, keep a backup copy, and note the restore in the report + CHAT.md.
  **ROOT CAUSE (verified 2026-08-12): the "mover" is the `audit-gpt-dispatch` cron (daily 07:00, no-agent), not a mystery actor.** It runs the PROFILE runtime copy of `audit_gpt_dispatch.py`, which had STALE absolute paths (`/Users/ted/Projects_GPT/Audit/Inbox` — retired 2026-08-10; `/Users/ted/Operations/...`). Script logic: read `dispatched/` → create Audit GPT inbox item → **move candidate to `done/`**. Timeline of a real case: Aug 7 07:00 created inbox item + moved candidate (first "undocumented move"); Aug 8 "no new" (candidate absent); Aug 9/10 "Already dispatched" (stale-path item existed → no move); **Aug 11 07:00 stale-path item gone (retired dir deleted) → RE-created + moved candidate to done/ again**. The source copy was repointed to canonical on 08-11 but the profile runtime (what cron runs) was never synced — classic script-sync gap. **Two things to fix, not one:** (1) sync the fixed script source→profile runtime (md5 match, re-run to prove no move), AND (2) consider whether the cron should be running at all — `audit_gpt_dispatch.py` auto-creates Audit GPT inbox items and moves candidates to `done/`, which violates a report-only v0 trial gate ("nothing dispatches without Ted/Codex approval"). Pause the cron (`hermes cron pause <id>`) to honor the gate until Ted/Codex approves dispatch. Also check sibling scripts for the same stale path: `audit_router.py` (daily 06:30) had the identical `Projects_GPT/Audit/Inbox` mkdir-resurrection bug — a write to a missing folder recreates it, so the retired dir reappears every morning even though reads look clean. Sync BOTH scripts. **When the pattern intensifies AFTER operationalization (e.g. 4 → 5 stop-hook alerts on the same path), FOLD the new alert into the existing candidate** — append to `change_refs`, extend the dedupe window (e.g. `...20260804-20260808`), update the candidate question to the new session count. Do NOT create a second request for the same question: it splits the evidence trail and risks dedupe confusion. Verify the final JSON parses (byte-level check; beware display-layer `****` artifacts that are not in the actual file — `has 0x2a: False` proves clean). **Inverse trap (real case 2026-08-10):** the display layer can also MASK a clean value to LOOK corrupted — a dedupe_key that reads `202****0804` in tool output is actually `20260804` in the file (digit run masked). Before "fixing" an apparent corruption, byte-dump the specific field (`val[44:].hex(' ')`) and check `b'****' in val` on the raw slice; a legitimate glob asterisk (e.g. `source_surface: _AI_Inbox/Audit_Alert_High_Signal_*.md`) also makes `has 0x2a: True` while the key itself is clean. If the key is clean, do NOT add fix-notes — restore from a pre-edit backup (cp first, always) and keep the file byte-identical to its verified state. See `references/audit-candidate-placement-restore-2026-08-08.md` for the full worked example.
- **Unpinned cron model-drift error — two fix paths:** When a cron errors with `RuntimeError: Skipped to prevent unintended spend: global inference config drifted since this job was created (provider 'X' -> 'Y'; model 'A' -> 'B')`:
  - If the job IS a no-agent script (pure Python, no LLM needed): fix with `hermes cron edit <job_id> --no-agent`. This removes the model dependency entirely. Example: `substrate-archive-reports` was erroring until set to no-agent mode on 2026-07-05.
  - If the job is agent-based (needs an LLM): pin it via `hermes cron edit` — but note that `--provider` and `--model` are NOT valid on `hermes cron edit`; they're top-level `hermes` CLI args. To re-pin, delete and recreate the job, or set explicit provider/model in the job's prompt/config.
  - If the job has no script and you try `--no-agent`: fails with `Cannot set no_agent=True on a job without a script. Set script in the same update, or on the job first.`
  - **CLI reminder:** `hermes cron update` does NOT exist — the correct subcommand is `edit`.
  - Unlike stale delivery errors (which self-clear on next run), model-drift errors persist every cycle until the job is explicitly fixed. They do NOT self-clear.
  - See also `cron-job-health-audit` skill pitfall 8a (LLM-driven job variant).
- **No-agent cron silent output failure** — A no-agent cron can exit 0, pass cron health pulse, and pass watchdog run-integrity while its actual output (archived files, generated reports, moved data) has been stale for weeks. The health checks verify the cron *started and ran* — they do not verify the cron *did anything useful*.

  **Diagnostic steps (in order of reliability):**
  1. **Read the cron's actual output log** — check `~/hermes-public-config/cron/output/<job_id>/` for the latest run's output file. The log contains the actual list of files processed/moved/generated. This is the single most reliable signal.
  2. **Check the output landing zone's content** — list actual files inside the output directory, not just the directory's modification timestamp. Directory mtime is updated any time a file is added or removed (including by the cron's `mkdir(exist_ok=True)` call) and can look "fresh" even when nothing useful happened.
  3. **Check cron exit history** — exit 0 plus frequent runs is no evidence of useful output.

  **Common blind spot: directory mtime is misleading.** A cron that runs daily and calls `ARCHIVE_DIR.mkdir(exist_ok=True)` at the top will touch the directory's metadata, making it look recently modified. But the files *inside* the directory may be weeks old. The directory timestamp reflects only the latest file move as its newest timestamp; older accumulated content stays untouched. To detect this: list the newest actual files inside the directory, not `ls -ld` the directory itself.

  Real case (2026-07-26): `substrate-archive-reports` appeared broken because the Archive subdirectories (Audit, root) hadn't been updated since June. But the cron was actually working — it moves files flat into `Archive/`, and the cron's output log at `hermes-public-config/cron/output/c46790a1ffc8/2026-07-26_06-00-11.md` showed 6 files archived that day. The diagnostic error was checking subdirectory mtime instead of reading the cron's output log.

  See `references/archive-cron-silent-failure-2026-07-26.md` for the full worked example including the correction.

## Verification

After running:
1. Check `~/Operations/reports/meta_agent_YYYY-MM-DD.md` for the summary
2. Spot-check 2-3 Project Rooms to verify updates make sense
3. Review `~/Operations/state/meta_agent_log.jsonl` for any errors

## Deep Integrity Scan Variant

For weekly system-wide health beyond Project Rooms (cross-profile cron inventory, script sync integrity, LATEST report freshness, inbox change detection), see `references/deep-integrity-sweep-pattern-2026-07-04.md`. Implementation: `~/.hermes/scripts/substrate_integrity_sweep.py`.

## Behavior-Rule Enforcement Sweep Variant

When a documented rule keeps being violated by actors ("that is the rule, nobody bothers following it"), a reminder does not work — build a **baseline-diff enforcement sweep**: baseline all current violations once, then silently alert only on NEW ones each day (exit 0 silent when clean, exit 1 + `_AI_Inbox` alert on new). Canonical-target symlinks are exempt. This is poka-yoke applied to actor discipline (orphan sweep + output-pattern-intercept). Real case 2026-08-02: `symlink_home_path_sweep.py` built after 39 home-path symlinks appeared in a week despite the de-alias rule. See `references/behavior-rule-enforcement-sweep-2026-08-02.md` for the guard shape, the SYMLINK_SWEEP_HOME testability hook, the hermetic-verification pitfall (canonical root must be OUTSIDE the home sandbox), and the ledger/escalation integration.

**Verify a canonical target exists before recommending repoint.** The de-alias remedy presupposes a canonical `/Volumes/Extra/Substrate/...` twin exists. If the target directory is home-path-resident with no Extra twin (real case 2026-08-03: `~/Control` — no `/Volumes/Extra/Substrate/Control`), repointing is impossible and the disposition is a rule-scope question for Ted (is the directory exempt from de-alias, or does it need an Extra home?), NOT a mechanical fix. Run `ls -d /Volumes/Extra/Substrate/<dir>` before writing "repoint" as the next action. Also: baseline inventory can dwarf the new set (62 total violations vs 2 new on 08-03, all following the same `~/.claude/hooks → ~/Control/claude/hooks` pattern since May) — only NEW violations since baseline are actionable alerts; don't re-flag the baseline. See `references/system-health-guard-triage-2026-08-03.md` for the worked case plus the disk-pressure guard exhaustion triage (snapshots_after: [] means the guard can no longer hold the floor; re-measure Tier B sizes live before quoting the doc's numbers; if even full Tier B can't cross the floor it's decision-needed, not auto-cleanup).

**Third disposition class — canonical twin EXISTS but another actor's surface (real case 2026-08-08):** Not every new violation is the parked "no twin" rule-scope question. When `ls -d /Volumes/Extra/Substrate/<dir>` DOES find the canonical twin, the violation is mechanically fixable (repoint target verified), BUT if the symlink lives in another actor's wiring (e.g. `~/.claude/skills/` — Claude Code's surface), do NOT auto-fix. Surface it with the verified repoint target and route to the owning actor (CC/Ted): "canonical twin exists at X, repoint is mechanical, owner should repoint". Also check whether the home path itself is a symlink to canonical (real case: `~/Skills` → `/Volumes/Extra/Substrate/Skills`), which makes the violation "points through a home alias to canonical" — still a path-hygiene violation per the de-alias rule, still repointable to the direct canonical path.

**Escalation when repoint-only keeps losing (real case 2026-08-14):** After ~2 weeks of daily "N new symlinks, repoint + root-cause" routing, the count jumped 3 → 10 NEW in one day (all `~/.claude/skills/* → ~/Skills/*`, every canonical twin verified to exist). Repoint passes had worked (CC repointed Aug 10), then new emissions resumed and grew — repoint-only is not a durable fix against an active installer, and CC's new write-boundary guard (`precommit_block_alias_paths.py`, blocks alias-path CONTENT at commit) does NOT stop symlink CREATION — different enforcement surface. When the pattern escalates despite repeated routing, the disposition is no longer "repoint" — it's a root-cause demand on the owning actor (which installer/skill-sync keeps creating `.claude/skills/*` symlinks?) or a Ted rule-scope decision. Keep surfacing the raw count trend (08-08:1, 08-09:2, 08-10:3 → repointed → 08-12:1, 08-13:3, 08-14:10) so the escalation is visible, not just the latest number.

## Codex Automation Absorption

When moving work away from paid Codex automations into free Hermes scripts (identify → understand → build → verify → close), see `references/codex-automation-absorption-pattern-2026-07-04.md`. Real examples: overnight consolidation, contradiction check, morning briefing, header pilot review.

## Example: What a Meta-Agent Found

First run (2026-06-17):
- 33 Project Rooms scanned
- 24 active, 9 need review, 0 stale
- Review rooms: Agent_Continuity_State_Loss (17 days), CC_Codex_Coordination (21 days), Dashboard_Consolidation (21 days), etc.
- All 33 rooms updated with fresh timestamps

## Batch Quiet Workflow

When the sweep detects 3+ stale Active rooms simultaneously, execute the batch quiet workflow: read sweep report → triage each room → update CURRENT_STATE.md + PROJECT_ROOMS_STATUS.md → verify. See `references/batch-stale-room-quiet-workflow-2026-06-23.md` for the full procedure.

## Quiet Room Refresh Pass

When the meta-agent re-flags rooms that are *already* "Quiet until trigger" but >30 days since last state update. This is **not** a lifecycle change — it's a date stamp refresh so the sweep stops flagging them.

### When to trigger
- Meta-agent sweep shows 1+ rooms flagged as "Needs Action"
- Their CURRENT_STATE.md lifecycle state is already "Quiet until trigger"
- The "Card checked" or "Room state last updated" date is >30 days ago
- Ted explicitly says to refresh them ("do the pass to quiet everyone for a bit longer")

### Files to touch per room

| File | What to update |
|------|---------------|
| `CURRENT_STATE.md` | Bump `Card checked:` and `Room state last updated:` to today |
| `CURRENT_STATE.md` | Update the `_Quieted by Substrate:` stamp to today with note "refreshed for continued quiet" |
| `CURRENT_STATE.md` | Update `_Last meta-agent check:` to today |
| `NEXT_ACTION.md` | If it has a dated `Updated:` header, bump to today |
| `PROJECT_ROOMS_STATUS.md` | Bump the room's "Last updated" column and the file's `Last updated:` header |

### What NOT to touch
- Room lifecycle state (stays "Quiet until trigger")
- Room content, description, NEXT_ACTION logic, owner, boundaries
- Other rooms flagged as "Worth A Quick Look" (those are different — see Stale-Flag Triage)

### After the pass
- Confirm the meta-agent sweep no longer flags these rooms (it resets the 30-day clock)
- Log the refresh in CHAT.md under a session log entry
- Record which rooms were refreshed in the session output

### Real example (2026-07-07)
- 4 quiet rooms at 32-33 days refreshed: Clinical_CoCM_Development, Clinical_GPT_Development, Comparative_Systems_And_Long_Range, Workspace_Icon_System
- Each: CURRENT_STATE.md dates + quieted stamp updated, Clinical_GPT NEXT_ACTION.md date bumped, PROJECT_ROOMS_STATUS.md header + 4 rows updated
- Ted asked for it, done in one pass, ~2 minutes

## Cross-Actor Change Verification

When another actor (Claude, Codex, Homes Manager) makes changes to the Substrate — cron schedules, model routing, script files, config — verify those changes for correctness and side effects. Ted explicitly depends on this: "I'm depending on Substrate-Hermes to do this work. Make sure everyone else is doing them well and the system stays stable."

### When to trigger
- At session start, after reading CHAT.md and ACTIVE_INDEX.md, scan for recent changes by other actors
- When Ted mentions another actor made changes
- When you find a script sync gap, stale fact, or unexpected cron schedule

### Verification checklist
1. **Cron schedule changes** — Are new times outside DeepSeek peak windows? Are schedules valid cron syntax? Do any overlap or conflict?
2. **Model routing** — Are agent-based crons using `deepseek-v4-flash` via `deepseek` provider? Are any still on `openrouter/free`?
3. **Script sync** — Compare `~/.hermes/scripts/<script>.py` vs `~/.hermes/profiles/<profile>/scripts/<script>.py` for size mismatches and missing files. Cron runs from the profile scripts dir.
4. **Fact store staleness** — Check holographic memory for entries referencing retired models/actors (owl-alpha, GA-Hermes).
5. **LATEST report freshness** — Flag reports that haven't updated in 48h+.
6. **Cross-profile crons** — Check all active Hermes profiles for orphaned crons using retired models or missing scripts.

### When you find issues
- **Sync gaps** → copy the missing/outdated file. Canonical source is `~/.hermes/scripts/`; profile dir is the runtime target.
- **Peak window violations** → move agent-based jobs outside both windows. See "Model Picker Integration" for tradeoffs.
- **Stale facts** → update via fact_store(action='update').
- **Model routing drift** → update the cron job with correct provider and model.

### Trust tests (when a proposal is routed to you)
Another actor may send you a proposal to test whether you substantively engage. Signals: the proposal asks you to "review" but the content is clearly within your domain. Do NOT rubber-stamp. Push back on claims you can verify (room counts, file existence, readiness sequencing). Name specific discrepancies. A reply that says "looks good" without engaging the content is the failure mode.

See `references/cross-actor-verification-2026-07-04.md` for the worked example from this session (Claude cron changes + Homes Manager trust test).

## Relationship to Other Skills

- **hermes-operations** — cron management, gateway recovery, model resilience
- **pieces** — reading Pieces data (digest, conversations, LTM events)
- **session_search** — reading chat session transcripts
- **plan** — for designing new meta-agent systems
- **cron-job-health-audit** — cron fleet health auditing, specifically pitfalls 14 (peak windows) and 15 (script sync)

## When NOT to Use

- Single-source monitoring (use a simpler script)
- Real-time monitoring (use file watchers, not cron)
- One-off checks (just run the analysis manually)
- When the human wants to do the review themselves

The user has provided the following instruction alongside the skill invocation: [IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

## Script Output
The following data was collected by a pre-run script. Use it as context for your analysis.

```
[meta_agent] Starting sweep at 2026-08-15 04:30 UTC
[meta_agent] Pieces digest: found
[meta_agent] Recent sessions: 20
[meta_agent] Recent audit findings: 5
[meta_agent] Project Rooms found: 36
  AI_Memory_Fleet_Access: active
  Canon_Reshape: stale
  AI_Cost_Budgeting_System: active
  AI_Failure_Recovery_Methods: active
  Agent_Continuity_State_Loss: stale
  CC_Codex_Coordination: active
  Capability_Drift_Addressing: active
  Clinical_CoCM_Development: stale
  Clinical_GPT_Development: stale
  Clinical_PHI_Audit_Safety: active
  Comparative_Systems_And_Long_Range: stale
  Dashboard_Consolidation: active
  Data_Room_Discipline_Trial: active
  Digest_Research_Monitoring_Calibration: stale
  Digest_Verification_Candidate_Routing: active
  GPT_Fleet: stale
  Image_Factory_Development: stale
  Orchestration: stale
  Project_Room_Next_Action_Visibility: active
  Proposal_Context_Probe_Method: active
  QuickSave: stale
  Research_Coordination: review
  Skills_Architecture_Detection_Conversion: active
  Skills_Stewardship: active
  Substrate_Finance_Planning: stale
  Substrate_Inventory_and_Closeout_Automation: active
  Substrate_Origin_Story: active
  System_Maintenance: active
  System_State_Briefing: active
  Work_Notes_Decision_Intake: stale
  Workspace_Icon_System: stale
  Hermes_Operator_Integration: stale
  Project_Interface_Workspace_Flow: review
  Role_Library: stale
  Role_Runtime_Architecture: stale
  model_testbench: active
[meta_agent] Sweep complete. Changes: 2. Report: /Users/ted/Operations/reports/meta_agent_2026-08-15.md
[meta_agent] Report opened in MacDown 3000
```

Run the meta-agent sweep to check all Project Rooms for staleness and health. Execute /Volumes/Extra/Substrate/Operations/scripts/meta_agent_sweep.py, read the generated report under /Volumes/Extra/Substrate/Operations/reports/, and return only its actionable findings. This job is report-only. Do NOT create or update any Substrate_Daily_YYYY-MM-DD.md file and do NOT update Substrate_Monitor/CHAT.md; the separate no-agent substrate-daily-report job at 07:00 is the sole daily-report writer.

## Response

Sweep verified: `meta_agent_sweep.py` ran 2026-08-15 00:30 EDT (report-only, no room writes), report at `/Volumes/Extra/Substrate/Operations/reports/meta_agent_2026-08-15.md`. 36 rooms scanned; 18 flagged. Content-aware triage below — only actionable findings.

## Meta-Agent Sweep — Actionable Findings (2026-08-15)

### 1. Two Active rooms are actually LIVE — state file lags, refresh only (no decision needed)
- **Role_Runtime_Architecture** — `FLEET_TRACKER.md`/`ROLE_LAUNCH_REGISTRY.md` updated **Aug 13**, but `Card checked: 2026-07-14`. Live work happening; CURRENT_STATE.md just wasn't bumped.
- **Hermes_Operator_Integration** — `Hermes_Records/` modified **Aug 14 07:45** (Codex offload records), but `Card checked: 2026-07-15`. Same pattern.

Both are state-refresh jobs: bump `Card checked:`/`Room state last updated:` to today so the next sweep stops flagging them. No lifecycle change.

### 2. Two Active rooms approaching stale — need a Ted route (refresh vs quiet)
- **Project_Interface_Workspace_Flow** — Active, 26d since card check (07-20); last file writes Aug 11.
- **Research_Coordination** — Active, 26d (07-20); CURRENT_STATE.md last touched Aug 11.

These are the same two bumped in the 2026-07-08 cleanup pass — the 30-day clock has nearly run out again. Route: quick refresh pass or move to quiet.

### 3. Ten quiet-until-trigger rooms re-flagged >30 days — date-refresh candidates ONLY if Ted routes the pass
Agent_Continuity_State_Loss (50d), Orchestration (47d), Work_Notes_Decision_Intake (47d), Digest_Research_Monitoring_Calibration (43d), Image_Factory_Development (43d), Clinical_CoCM_Development (39d), Clinical_GPT_Development (39d), Comparative_Systems_And_Long_Range (39d), Workspace_Icon_System (39d), Role_Library (38d).

Expected re-flag pattern — the 07-07 quiet refresh reset a 30-day clock and it's now expired. **No lifecycle changes.** If Ted wants them quieted for another stretch, say the word and I'll do the date-stamp pass (CURRENT_STATE + PROJECT_ROOMS_STATUS + quieted stamp).

### 4. Two unknown-state rooms — genuine Ted decision (I won't auto-touch)
- **Canon_Reshape** (Brain) — 44d, no live strand (all files Jul 2). **Canon = CC domain, not mine** — needs CC/Ted decision on refresh vs quiet/archive.
- **Substrate_Finance_Planning** (Commons) — 44d, but `Reports/` has Aug 8 activity. **Finance = protected surface for me.** Decision needed: refresh state or quiet.

### 5. Settled/historical — verify only, no action expected
GPT_Fleet (43d) and QuickSave (42d) — confirm PROJECT_ROOMS_STATUS.md rows agree with "Settled/historical"; no lifecycle change.

### Context (not actionable from this job)
- 5 Audit GPT runs in the last 7 days (findings 3/4/5/5/0) — log: `Substrate/Audit/RUN_LOG.md`.
- Sweep reported 2 status changes vs last run; report-only so no files were written.

**Bottom line:** No emergency. The highest-value action is refreshing the 2 live-but-stale Active rooms (#1) — that's bounded and safe if routed. Everything else is either a Ted route (items 2 and 4) or waiting on an explicit quiet-pass instruction (item 3).
