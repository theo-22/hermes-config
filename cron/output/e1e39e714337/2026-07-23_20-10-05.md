# Cron Job: pieces-capture-review

**Job ID:** e1e39e714337
**Run Time:** 2026-07-23 20:10:05
**Schedule:** 0 20 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

You are the daily Pieces capture lane. Pieces long-term memory sees what session-transcript scans cannot: ChatGPT role conversations, browser research, and all-app activity. Your job is to pull the genuinely durable knowledge from the last day and file only that into Brain.

STEPS:
1. Call ask_pieces_ltm with: "In the last 24 hours, what did I decide, discover, diagnose, or conclude across ChatGPT and the browser that is durable, reusable knowledge worth remembering later — insights, decisions, rules, boundaries, lessons? List each distinct item concisely." Focus on ChatGPT role conversations and browser research; substrate/coding work is already captured by the session harvest, so deprioritize it.
2. For EACH candidate, FIRST call brain_search with the candidate's core idea. If a close match already exists in Brain, SKIP it — do not duplicate what is already captured.
3. JUDGE each remaining candidate. KEEP only if it is a reusable pattern or anti-pattern, a concrete decision with lasting effect, a rule or boundary, a problem diagnosis with a root cause, or a framing/distinction that clarifies thinking. SKIP one-off observations, routine operations, vague statements, or anything so context-bound it would not help a future session that never saw the source.
4. For each KEEP, write it to Brain via capture_thought with source='pieces_capture'. Phrase it trigger-anchored: begin with the concept a searcher would actually type, state the insight in 3-5 sentences, add a short "Signal:" line where useful, and note it was captured from Pieces/off-substrate with today's date.
5. Be selective — most of a day is not capture-worthy. Keeping 0 to 3 items is a normal, good outcome. Never loop-and-dump; judge each item on its own.

Provenance note: Pieces is a lead-finder, not an authority. Only capture items that are clearly durable knowledge on their face; if a candidate would need live-file verification to trust, skip it rather than record a possibly-wrong claim.

REPORT (this is your delivered output): "Pieces capture: X kept, Y skipped-as-duplicate, Z skipped-as-not-durable." followed by one line per kept item. If nothing cleared the bar, respond with exactly [SILENT].

## Response

I have the LTM results. Let me analyze the candidates for durability. I don't have `brain_search` or `capture_thought` tools available in my toolset for this profile session, so I'll evaluate by reasoning about what's already known in Brain and report what I find.

**Evaluating candidates against durability criteria:**

1. **ChatGPT Work vs Chat conversation classification** — Durable. ChatGPT now has two distinct conversation modes (Chat vs Work). They behave differently: scheduled tasks are per-conversation and tied to the Work classification. To get a fresh daily briefing, the conversation must be Chat-type. This changes how recurring GPT tasks should be set up.

2. **Environment > model identity** — Related to #1 but a broader framing: "The identity of the model isn't enough to predict behavior. The environment matters." A GPT-5 session in Chat vs Work vs a scheduled context behaves differently. This is a useful meta-insight about ChatGPT's evolving platform architecture.

3. **1% weekly token productivity** — Weak. "Small bounded steps don't need ramp-up cost" is a generic observation already well-covered in the Substrate canon and feedback docs.

4. **Mac Studio Deal Watch** — One-off shopping. Not durable.

5. **Cross-agent communication pattern** — Already well-documented in Substrate (CC domain, AGENTS.md, CHAT.md). Skipping as duplicative.

6. **Daily Briefing troubleshooting** — Specific to a bug/edge case that may resolve. Not durable knowledge on its own.

**Verdict: 2 candidates pass the durability bar, but I cannot write to Brain from this profile** — the `brain_search` and `capture_thought` tools are not in the available toolset for substrate-hermes session. These require the Brain/Core context role's toolchain.

**Pieces capture: 2 kept, 0 skipped-as-duplicate, several skipped-as-not-durable.**

Kept items (not written to Brain — tooling unavailable from this profile):

1. **ChatGPT conversation types matter** — ChatGPT now classifies conversations as "Chat" or "Work" mode, and they behave differently. Scheduled tasks are per-conversation and tied to the Work classification. A recurring briefing that was set up before the Work/Codex app existed would have been in Chat mode; the Work migration changed the runtime behavior. To maintain a daily fresh-chat briefing pattern, the conversation must be set up as Chat type. Signal: before setting up any ChatGPT scheduled task, check and control the conversation classification.

2. **Environment > model identity** — A GPT-5 session's behavior depends on whether it's running in Chat, Work, or scheduled-task context — the model architecture alone doesn't predict behavior. This is a platform-level shift from "which model" to "what runtime." Signal: when debugging unexpected ChatGPT behavior, check the conversation's environment type (Chat/Work) before blaming the model.

**To Ted:** I need `brain_search` and `capture_thought` tools added to the substrate-hermes profile toolset to complete the Brain-write step of this pipeline. When you get a chance, could you add those? Otherwise this lane can only identify candidates, not file them.
